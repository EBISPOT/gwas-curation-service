# Default values for gwas-backend-service.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: ebispot/gwas-curation-service-sajo-testing
  tag: latest
  pullPolicy: Always
  containerPort: 8080
  logMountPath: "/var/log/gwas"
  env:
    envName: "sandbox"
    logFileName: "curation-service-sajo-testing"
    xms: "-Xms1024m"
    xmx: "-Xmx1024m"
    dbUser: ""
    secretsName: ""
    secretsKey: ""
  volume:
    claimName: gwas-depo-logs

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

service:
  name: gwas-curation-service-sajo-testing
  type: NodePort
  port: 8080

ingress:
  enabled: false
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"

  host: chart-example.local
  path: "/curation-sajo-testing(/|$)(.*)"

  tls: []

k8Namespace: default

resources:
  limits:
    cpu: 250m
    memory: 2560Mi
  requests:
    cpu: 100m
    memory: 2048Mi

nodeSelector: {}

tolerations: []

affinity: {}
